# 竞速功能：本地运行与部署

Cloudflare 迁移版本已实现，使用 Workers + Durable Objects + D1 与 WebSocket。部署步骤及容量限制见 [cloudflare/README.md](cloudflare/README.md)。下文为原 Node.js / Ubuntu 方案，仍可用于本地运行。

## 本地使用
需要 Node.js 22 或更新版本。在项目目录执行 `npm start`，打开 http://127.0.0.1:5173 。两个模块的第一张关卡卡片为“竞速挑战”。

两名玩家须使用不同浏览器或独立浏览器配置（无痕窗口也可），否则会共享本地身份。昵称可重复，成绩身份保存在浏览器中；清除站点数据后不能恢复旧身份。

局域网测试：PowerShell 执行 `$env:HOST="0.0.0.0"; npm start`，另一台电脑访问主机局域网 IP 的 5173 端口。需要主机防火墙允许该端口；不要把 localhost 发给另一台电脑。

## 已实现规则
- 个人竞速每次随机 5 题，首次倒计时 3 秒，服务器计时并校验完整布局；后续换题时间包含在总用时中。
- 双人 BO5 / BO7 / BO9，同题独立操作，双方加载完后倒计时 3 秒；先提交合法解的一方得分。先达到 3 / 4 / 5 分结束，否则题目耗尽按比分判定，可平局。
- 双方都申请跳过才跳题；可撤销申请。每题 5 分钟超时，联机不计分换题，个人结束挑战。
- 比赛中失联超过 20 秒判负，两人均失联则无胜者；加载超过 30 秒结束，只有一人已加载时该玩家获胜。主动离开立即判负。
- 赛后提供合法解与种子，种子可在对应游戏的自定义关卡中导入。排行榜和个人历史按游戏、难度区分。
- 简单模式遵守相应对称条件，所有题目由合法解生成并再次验证；混沌模式浮空回收气球为 1–25 个（5×5 网格容量）。

## 公网部署
GitHub Pages 只承载静态前端，无法运行此服务端。当前没有配置公网后端，因此本地成绩不是已上线的全球排行榜。

部署 `server.mjs`、`backend/`、`dist/` 到支持常驻 Node.js 的服务器，或使用项目 Dockerfile。无需运行时 npm 依赖。只运行一个实例；房间保存在内存，服务器重启会结束所有进行中的比赛。

环境变量：
- `HOST`：容器建议 `0.0.0.0`。
- `PORT`：默认 `5173`。
- `RACE_DATA_FILE`：默认 `.race-data/store.json`，必须绑定持久化磁盘，保存匿名身份和成绩；定期备份。
- `ALLOWED_ORIGINS`：逗号分隔前端来源，默认包含 `https://njucodenoob.github.io` 和本地 5173 地址。不要设置为任意来源。

为服务端配置 HTTPS。将 `dist/race-config.js` 中 `window.RACE_API_BASE` 设置为服务端 HTTPS 地址，再发布 GitHub Pages；也可以在竞速菜单“连接设置”中手动填写。所有玩家需要连接同一个后端。健康检查为 `/api/health`。

## 校验范围
服务端掌握题目、开始时间、答案判定及比分，客户端不能直接提交用时。活动比赛不公开种子或参考解；结束后公开供复盘。时间按服务器接收合法提交的顺序判定，网络延迟会影响极接近的成绩。

当前使用 HTTP 状态轮询（350 毫秒），并非 WebSocket。随机题的实际难度不完全相同，排行榜按模式分组，不能当作严格同题竞技评分。校验可以拦截改时间、改元件和重复提交，但不能保证识别自动解题程序。昵称只做长度与控制字符限制，未接入完整内容审核。大规模公开运营前需补充账号恢复、审核、限流监控及数据库服务。

运行 `npm test` 进行生成器、服务端与模拟双浏览器交互测试，`npm run check` 检查脚本语法。
